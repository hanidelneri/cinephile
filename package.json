{
    "name": "cinephile",
    "version": "0.0.1",
    "type": "module",
    "description": "This is an example of a Crawlee project.",
    "dependencies": {
        "crawlee": "^3.0.0",
        "playwright": "*"
    },
    "scripts": {
        "crawl:yorck": "npx tsc && node src/crawlers/yorck/crawler.js",
        "crawl:cinestar": "npx tsc && node src/crawlers/cinestar/crawler.js",
        "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
    },
    "author": "Hanie",
    "license": "ISC",
    "devDependencies": {
        "typescript": "^5.4.3"
    }
}
